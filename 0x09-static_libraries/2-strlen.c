#include "main.h"
#include <string.h>

/**
 * _strlen -  returns the length of a string
 * @*S: string
 * @s: string input
 * Return: length
 */

int _strlen(char *s)
{
	int len;

	len = strlen(s);
	return (len);
}
