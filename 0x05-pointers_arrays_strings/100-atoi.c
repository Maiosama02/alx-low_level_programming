#include "main.h"
#include <stdio.h>
#include <string.h>
#include <stdlib.h>

/**
 * _atoi - convert a string to an integer
 * @s: string
 * Return: atoi
 */

int _atoi(char *s)
{
	int i;

	i = atoi(s);
	return (i);
}
